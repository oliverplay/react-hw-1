import React from 'react';

const Statistics = ({ data }) => {
  console.log('🚀 ~ file: Statistics.jsx:4 ~ Statistics ~ data:', data);

  return <div>Statistics</div>;
};

export default Statistics;
