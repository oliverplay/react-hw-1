import React from 'react';

const Transactions = ({ data }) => {
  console.log('🚀 ~ file: Transactions.jsx:4 ~ Transactions ~ data:', data);

  return <div>Transactions</div>;
};

export default Transactions;
